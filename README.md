# PyGramm
Javascript Lexer and CFG

Usage
```bash
usage: main.py [-h] [--parse] [file]

Lex a JavaScript file.

positional arguments:
  file        Path to the JavaScript file to lex

options:
  -h, --help  show this help message and exit
  --parse     Parse tokens with CFG parser and print AST
```
